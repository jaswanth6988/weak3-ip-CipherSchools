/* Callback function */
function resultCalculator() {
   document.querySelector("main") = append(result);
}

function calculation (a, b, Callback) {
   let sum = a + b;
   cancelIdleCallback(sum);
}

calculation(5, 8, resultCalculator);
